{
  "name": "@demo/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsnd --respawn --transpile-only --exit-child src/app.ts",
    "build": "tsc -d"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.0",
    "fastify": "^4.13.0",
    "fastify-zod": "^1.4.0",
    "zod": "^3.22.4",
    "zod-to-json-schema": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "ts-node-dev": "^2.0.0"
  }
}
